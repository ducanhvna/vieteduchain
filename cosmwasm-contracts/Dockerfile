# Dockerfile for building all CosmWasm contracts in this folder
FROM cosmwasm/workspace-optimizer:0.14.0
# This image includes Rust, cargo, wasm-opt, and all CosmWasm build tools
# Usage: docker run --rm -v $(pwd):/code cosmwasm/workspace-optimizer:0.14.0
